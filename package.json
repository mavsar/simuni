{
  "name": "simuni",
  "version": "2.0.0",
  "private": true,
  "description": "Simuni app (React + Node + SQLite)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w @simuni/server\" \"npm run dev -w @simuni/web\"",
    "build": "npm run build -w @simuni/web && npm run build -w @simuni/server",
    "start": "npm run start -w @simuni/server",
    "typecheck": "npm run typecheck -w @simuni/web && npm run typecheck -w @simuni/server",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.8.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
